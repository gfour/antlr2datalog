#include "../workspace/schema.dl"
#include "../logic/base-logic.dl"

.decl StringConstant(str: symbol, line:number)
.output StringConstant

StringConstant(str, line) :-
  LiteralContext_NONNUMERICLITERAL(_, _, str, line, _, _, _).
